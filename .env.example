# piLoci Environment Variables
# Copy to .env and fill in values. Never commit .env.

# === Required ===
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=change-me-generate-with-secrets-token-hex-32
SESSION_SECRET=change-me-generate-with-secrets-token-hex-32

# === Database ===
DATABASE_URL=sqlite+aiosqlite:////data/piloci.db

# === Qdrant ===
QDRANT_URL=http://qdrant:6333
# QDRANT_API_KEY=  # if Qdrant has auth enabled

# === Redis ===
REDIS_URL=redis://redis:6379/0

# === Embedding ===
EMBED_MODEL=BAAI/bge-small-en-v1.5
# EMBED_CACHE_DIR=/data/embed_cache

# === Server ===
HOST=0.0.0.0
PORT=8314
LOG_LEVEL=INFO
LOG_FORMAT=json
# WORKERS=4  # uvicorn workers (default: 1 in dev, 4 in prod)

# === Optional: SMTP (for email verification / password reset) ===
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your@email.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@piloci.example.com

# === Optional: Google OAuth ===
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# === Optional: Cloudflare Tunnel ===
# TUNNEL_TOKEN=  # cloudflared tunnel token (use Docker secrets in prod)

# === Dev only ===
DEBUG=false
RELOAD=false
